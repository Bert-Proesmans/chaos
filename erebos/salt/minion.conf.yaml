# Use the default configuration;
#
# 1 environment called 'base'
#   - State entry file located at /srv/salt[/top.sls]
#   - Pillar entry file located at /srv/pillar[/top.sls]

# `salt-call` command will not attempt to contact any masters. The minion 
# self-provisioned all state configuration. (See the configuration about
# environments.)
file_client: local